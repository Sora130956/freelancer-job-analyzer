# 项目工作流契约（Harness）

> 本文件是 AI 与开发者共同遵循的工作流约定。**每次会话开始 AI 必须先读 `.harness/state.md` 了解当前进度，收工后必须更新 `.harness/state.md`。**

## 硬性规则（不可跳步）

1. **先读状态再动手**：任何会话开场，先读 `.harness/state.md`，再读 `.harness/decisions.md`，确认"现在走到哪一步、下一步做什么"。
2. **按步骤推进，不跳步**：未完成当前步骤的产出物，不进入下一步。
3. **验收标准前置**：写任何实现代码前，必须先有 3-5 条可量化、可脚本检查的验收标准（第 3 步产出）。
4. **测试先于实现**：每个模块实现前先写测试（TDD）。
5. **验证先于完成**：声称某步/某模块完成前，必须运行验证命令并给出证据。
6. **状态落盘**：每完成一步、每完成一个模块，立即更新 `.harness/state.md`，不要只放在聊天里。

## 七步工作流

| 步骤 | 名称 | 产出物 | 完成标志 |
|------|------|--------|---------|
| 1 | 需求一句话 | `state.md` 第 1 步 | Who / What / Why 写清 |
| 2 | 可行性分析、技术选型 | `state.md` 第 2 步（或 `docs/feasibility.md`） | 明确能不能做 + 边界 + 风险 |
| 2.5 | **需求打磨**（基于调研结论） | `state.md` 更新版 + `docs/requirements.zh.md` + `docs/requirements.en.md` | 详细设计落盘，用户确认「能想象出最终产品长什么样」 |
| 3 | 验收标准 | `docs/acceptance.md` | 3-5 条可脚本检查标准，评审通过 |
| 4 | 整体设计 + 实现计划 | `docs/plan.md` | 模块拆分 + 顺序 + 依赖 |
| 5 | 生成 → validate → review | 代码 + 测试 | 每模块单测通过 + 整体验收通过 |
| 5.5 | Code review | `docs/review.md`（学习/开发目的） | 问题已记录并处理 |
| 6 | 决策记录 + 复盘 | `decisions.md` 追加 ADR | 回到第 1 步 loop |

## 每一步的详细约定

### 第 1 步：需求一句话
用 Who / What / Why 三段写清：**对于谁，需要做什么东西，达到什么价值。**
写进 `.harness/state.md` 第 1 步。

### 第 2 步：可行性分析、技术选型
- 回答：现有技术栈和资源能否实现目标价值。
- 必须产出**边界**：明确不做什么、风险点、外部依赖。
- 技术选型若涉及 A/B 选择，记录到 `.harness/decisions.md`。

### 第 3 步：验收标准（最重要）
- 3-5 条，**可量化、可脚本检查**，不能是主观描述。
- 示例（Excel 导出项目）：产出 Excel 有哪几个 sheet、每个 sheet 有哪些字段/数据/图表、数据与图表怎样算正确、空数据/异常输入时的行为。
- 写入 `docs/acceptance.md`，与用户评审通过后才进入第 4 步。

### 第 4 步：整体设计 + 实现计划
- 在写代码前做总体设计决策，拆分模块，制定实现顺序。
- 写入 `docs/plan.md`，包含：模块列表、每个模块职责、实现顺序、依赖关系、每模块的验证方式。

### 第 5 步：生成 → validate → review
- 按 `docs/plan.md` 分模块实现。
- 每个模块先写单测（TDD），通过后再写/补实现。
- 全部模块组合后，按 `docs/acceptance.md` 做整体验证。
- **验证命令必须有实际输出证据**，不靠"我觉得好了"。

### 第 5.5 步：Code review
- 学习/开发目的，每个模块完成后做一次 review（不要全部做完才 review）。
- 记录问题到 `docs/review.md`。

### 第 6 步：决策记录 + 复盘
- 记录"为什么选 A 不选 B"，用 ADR 形式追加到 `.harness/decisions.md`。
- 分析现存问题，回到第 1 步继续迭代。

## 状态维护规则

`.harness/state.md` 是**单一进度状态源**，必须保持最新：

- `## 当前阶段`：现在在第几步。
- `## 模块清单`：勾选完成情况。
- `## 下一步动作`：下一件具体要做的事。
- `## 决策`：本次会话新增的决策摘要（详情在 decisions.md）。

## 跨会话衔接

新会话开场，AI 必须：
1. 读 `.harness/state.md`。
2. 读 `.harness/decisions.md`。
3. 用一段话复述当前进度和下一步，等用户确认后再继续，不抢跑。
